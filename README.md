# ptsspammer

This will work on users logged into the same linux machine with ssh sessions - by piping into their shell sessions pts.


to install bash scripts.

git clone https://github.com/Jekyllz/ptsspammer.git

cd /ptsspammer

chmod +x pts_spammer.sh

./pts_spammer.sh

